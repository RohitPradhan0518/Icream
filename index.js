/*
let baseprice = 0;
let taxAmount = 0;
let totalDue = 0;
const TAX_RATE = 1.0828;


function submitOrder(
    scoopCount,
     containerType, 
     hasSprinkless, 
     hasFudge, 
     hasWip, 
     hasCherry
     ){*/

     submitOrderButton.onclick = ()=>{
        const sel
        submitOrder(
            scoopCount,
            document.querySelector("input[name'containerType']):checked");
            containerType, 
            hasSprinkless, 
            hasFudge, 
            hasWip, 
            hasCherry
        )

     }